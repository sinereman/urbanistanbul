// Ticker metni etkinliklerden dinamik oluşturulur
// Hugo build sırasında data.js ile beslenir
document.addEventListener('DOMContentLoaded', function() {
  var ticker = document.getElementById('tickerInner');
  if (!ticker) return;
  // Ticker içeriği Hugo tarafından baseof.html üzerinden doldurulur
  // Bu dosya fallback olarak çalışır
});
